version: 2.1

jobs:
  deploy-dev:
    docker:
      - image: cimg/base:stable
    steps:
      - run:
          name: "dev"
          command: "echo devlelop"
  deploy-stg:
    docker:
      - image: cimg/base:stable
    steps:
      - run:
          name: "stg"
          command: "echo stg"


workflows:
  deploy-workflow:
    jobs:
      - deploy-dev:
        filters:
          branches:
            only: 
              - develop
      - deploy-stg:
        filters:
          branches:
            only:
              - staging
      
